# 2026-03-28 工作日志

## 设置面板重构 + 图片发牌文件上传

### 完成的工作

1. **设置面板分组重构**
   - 将设置面板分为"叫牌设置"和"发牌设置"两个独立组
   - 用竖线 (Divider) 分隔两组，布局更清晰
   - 叫牌设置：模式、发牌人、人类位置、备用模型
   - 发牌设置：发牌方式、自定义、图片、截屏按钮

2. **按钮样式统一**
   - 字体大小统一为 0.875rem（与下拉框一致）
   - 边框颜色统一为 rgba(0, 0, 0, 0.23)
   - 高度统一为 40px，内边距统一
   - 移除文字大写转换

3. **图片发牌改为文件上传**
   - 前端：添加"浏览..."按钮，使用文件选择器选取图片
   - API服务：使用 FormData 上传文件
   - 后端：使用 UploadFile 接收文件，保存到临时目录处理后自动清理
   - 解决浏览器安全限制导致的文件路径问题
   - 安装依赖：python-multipart

### 遇到的问题
- API 服务端口冲突，需要手动重启
- 豆包 API 配置需要检查

### 修改的文件
- `web/src/App.jsx` - 设置面板重构、图片发牌对话框
- `web/src/services/api.js` - imageDeal 函数改为文件上传
- `api/main.py` - 添加 UploadFile 支持文件上传
- `CHANGELOG.md` - 添加更新记录
- `MEMORY.md` - 添加项目状态记录
