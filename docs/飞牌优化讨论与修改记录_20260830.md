# 飞牌优化讨论与修改记录（2026-08-30）

> 状态：**记录中，未提交（不 git commit）**。4♥ 例牌已打出飞牌成约；6♠ 另一副牌仍失败，待继续研究。
> 关联文档：[飞牌vs拔AK\_4H双飞时序研究.md](./飞牌vs拔AK_4H双飞时序研究.md)（同副牌的前置研究）、[教训\_临界过滤与飞牌识别的废弃.md](./教训_临界过滤与飞牌识别的废弃.md)（2026-08-26 废弃了 v1.68 飞牌干预——本文记录为何今日又重新评估该方向）。

***

## 一、缘起：用户的三个核心问题

1. "是否该把 DD+αμ 顺序调成先αμ、残局用DD？"——先跑成功率口径对比实验验证（未动架构）。
2. "如果识别到飞牌结构，直接飞不就行了吗？"——引出识别方法讨论。
3. 用户提出完整实战思想：**早期不猜、能拖就拖；拖不下去被迫飞；一旦启动飞牌，轮到队友必须接应（强制完成）**，因为错过了就再无飞牌机会。

## 二、实验一：成功率口径 vs 平均墩口径（结果推翻了预期）

在 4♥ 例牌（南♥AKJT98+北♥32，残局7张/手，飞牌决策点北♥3-东♥4-南跟）重采样 5×400 世界：

| 口径        | 拔♥A   | 飞♥J   | 首选     |
| --------- | ----- | ----- | ------ |
| 平均墩       | 6.327 | 6.210 | 拔A     |
| 成功率(≥10墩) | 28.2% | 19.0% | **拔A** |

- **关键结论：换成功率口径不能救飞牌**。2000 世界分类：J独胜=102、A独胜=286（2.8倍）。拔A失败常惜败9墩，飞J失败连9墩都保不住——均匀先验下拔A确实期望更优。

- **路线条件化才是关键**：条件到"飞牌可成"世界集（J成+J独胜=380）时，飞J 100% vs 拔A 73.2% → 飞牌登顶。人类坐庄本质是"把世界条件化到能成约的分布"，引擎是"均匀先验期望最大化"。

- 脚本：`scripts/_success_rate_compare.py`（成功率/平均墩/条件化四类分析）。

## 三、识别方法之争：方法A（冲突检测）失败，方法B（静态间张）成功

- **方法A（冲突检测）**：候选对逐世界成败向量互斥（你成我败/我成你败都>0）→ 飞牌结构。实测**只在 L2（飞牌点）检出，L1（北首出）零检出**——因为 L1 时飞牌牌（♥J）在庄家手中不在候选里，检测失效。作废。

- **方法B（静态间张，用户提出）**：合并庄家+明手每花色，从高到低找"可飞对象"缺失大牌 M（K/Q/J）；有上方控制张（>M）+ 下方≥T飞张 + 合计≥4张 → 该花色可飞。缺A/缺≥3/大牌齐全/无飞张不飞。**23/23 用例通过**（`scripts/_finesse_structure_chklist.py`），真实牌局精确识别 ♥ 缺Q。

- **教训**：飞牌结构识别必须在"决策前"静态完成，不能依赖候选内互斥——飞牌主角经常不在当前出牌人的候选里。

- 方法A脚本：`scripts/_finesse_detect_test.py`（两方法对比）。

## 四、已实现的代码修改（未提交）

### 4.1 config.py（全局开关与阈值）

```
FINESSE_DEFER_ENABLE      = True   # 总开关
FINESSE_DEFER_RATIO       = 0.90   # DD：次优无关牌/榜首 ≥90% 允许拖延
FINESSE_DEFER_RATIO_ALPHA = 0.80   # αμ：≥80%（≈用户指定成功率差20%）
```

用户决策：阈值用\*\*比值（相对成功率）\*\*统一跨计分制；αμ 单独 20%（更敢拖，因其 M=1 时本就是成功率口径）。用户强调"差距不能太大，差距大说明概率优势明显应该出"。

### 4.2 play\_service.py（新增7个方法，接入 DD/αμ）

| 方法                                           | 职责                                         |
| -------------------------------------------- | ------------------------------------------ |
| `_detect_finesse_struct(state)`              | 方法B：静态间张识别飞牌结构，返回 `{花色: {对象, 说明}}`         |
| `_is_finesse_struct_card(card, fs)`          | 候选牌是否结构相关（**全花色一刀切**：飞牌花色内所有牌都算，含A/K）      |
| `_finesse_obj_name(m)`                       | 对象 rank 数值 → 牌名                            |
| `_defer_candidates(candidates, fs, ratio)`   | 榜首为结构牌 + 次优无关牌比值≥阈值 → 返回建议无关牌              |
| `_apply_finesse_defer(state, result, ratio)` | 应用"拖延"：改选无关牌，写入 full\_output\["飞牌延迟"]      |
| `_finesse_commit_check(state, fs)`           | 强制接应判定：同伙已在该花色启动 + 我有飞张(J/T) + 我有更高顶张(A/K) |
| `_apply_finesse_commit(state, result)`       | 应用"强制接应"：改出飞张，写入 full\_output\["飞牌接应"]     |

接入位置：

- `_dd_play`：庄家方视角（current\_player ∈ 庄家/明手）时，先 `_apply_finesse_commit`（强制接应），未触发再 `_apply_finesse_defer`（拖延）。

- `_alpha_mu_play`：同上，但拖延用 `FINESSE_DEFER_RATIO_ALPHA`。

- 防守方出牌**不干预**（门控 current\_player ∈ (declarer, dummy)）。

### 4.3 验证脚本

| 脚本                                      | 验证点                       | 状态      |
| --------------------------------------- | ------------------------- | ------- |
| `scripts/_finesse_structure_chklist.py` | 方法B 23用例（单飞/双飞/隔位间张/反例）   | 23/23 ✓ |
| `scripts/_finesse_defer_verify.py`      | 识别+分类+拖延+阈值+全流程+防守方门控     | 5/5 ✓   |
| `scripts/_finesse_akj_verify.py`        | AKJ 中拔A是否被正确拖延            | ✓       |
| `scripts/_finesse_commit_verify.py`     | 强制接应（北♥3→东♥4→南必须♥J压♥A）+边界 | 4/4 ✓   |

## 五、AKJ 教训（重要，防反复）

- **错误假设**（曾建议）：飞牌花色内"兑现级（A/K）"不是结构牌，不用拖延。

- **用户纠正 + 实测确认**：**拔A/K同样烧明手短将方进手、毁掉双飞结构**。例牌南♥AKJT98+北♥32：兑现A/K强制明手跟将，♥3♥2耗尽，东♥Q654无法双飞。

- **最终判据**：飞牌花色**全花色一刀切**都算结构相关（含A/K）。差距大时才豁免（比值<阈值不干预=概率优势明确）。

- **边界**：结构识别只看花色；差距阈值兜底"该飞却拖"的过度保守。

## 六、强制接应（finesse\_commit）——用户发现漏实现后补齐

- 首次实现在 play\_service 时只做了"延迟拖延"，**漏了"强制接应"**。用户实测（北已打♥3启动飞牌，南仍出♥A）指出"南家必须完成飞牌而不是出A"。

- 补齐：`_finesse_commit_check` 判定——当前墩同伙（庄家/明手）已出飞牌花色牌 + 本家有飞张（<对象且≥T）+ 本家有更高顶张(A/K) → 必须出飞张压制顶张。

- 用户牌局实测通过：北♥3-东♥4-南 → 强制出♥J 压♥A。

- **注意**：强制接应的检测依赖 `state.current_trick` 在同一墩内逐家调用时正确传递（北出♥3时那次调用 threshold 内 current\_trick 已含♥3），后续若遇检测失效需排查此状态链。

## 七、与《教训\_临界过滤与飞牌识别的废弃.md》的关系（诚实记录）

2026-08-26 该文档结论：飞牌感知 v1.68 被移除，理由是"DDS 已算出最优、飞牌难在信息不在选择、唯一正解是采样约束"；留下的方向是"飞牌交互只做解释层不干预决策"。

**今日为何重新评估**（不矛盾，需区分）：

1. 废弃文档的 DDS 论证**正确**——单世界内 DDS 算对飞/拔（飞=10/拔=9），今日实验再次确认。
2. 今日新增论据在**聚合层**：2000 世界中 A独胜(286) 是 J独胜(102) 的 2.8 倍，均匀先验期望**结构性地**稀释"双飞时序"这类方向赌注——DDS 单世界正确 ≠ 期望聚合符合坐庄意图。这与废弃文档"约束足够时采样会自然排除"的论点是**互补**的：约束管"关键张位置偏向"，今日机制管"即使分布对称，也要按计划完成时序（不中途抢跑/梭哈）"。
3. 用户主导确立了**新立场**：规则式干预（识别→拖延→强制接应）是显式注入的"打牌优先序"，不是"教引擎选正确牌"——这点与废弃文档第 3 节"识别飞牌→干预决策是多余的"构成**观点分歧**，以用户今日确认的实现为准，但需在 6♠ 验证失败后复核是否要保留。

## 八、6♠（Bridge Master L2 A-13）排查：4 个 bug 已定位并修复

- 4♥ 例牌已能正确飞牌成约；6♠ 旧版策略失败，排查后发现**4 个 bug**（非阈值/识别漏报问题，而是实现缺陷）：

  1. `_FINESSE_R2V` 缺失 `'10'` 键 → ♠10 被误分类。**当前核查：rank 体系为 'T'，R2V 键齐全，无此问题。**
  2. **跨花色混入**：♦J/K 被错误计入♠ 飞牌候选。**当前核查：已被"飞张必须 ∈ 合法可出牌（跟牌规则）"顺带覆盖。**
  3. **已出大牌被误标为缺失**：`_detect_finesse_struct` 未考虑已打出的牌。
  4. **高花引牌被误判为飞牌启动**：任何飞牌花色引牌都当"启动"，未限制为同伙小牌引牌。

- **⚠️ 2026-08-30 曾完成修复（`seen_cards→present_set`、同伙 ≤9 限制、`_6s_verify.py`** **3/3 通过），后丢失**（8/31 核查：`_6s_verify.py` 不存在，代码无对应逻辑）。

- **8/31 已核查并重新补回**（play\_service.py）：

  - `_detect_finesse_struct`：增加已打出大牌感知（present = 手牌 ∪ 已出牌；已出的 K/Q 不再是"缺失对象"）。

  - `_finesse_commit_check`：仅当同伙在当前墩出过飞牌花色的 ≤9 小牌才算启动飞牌（A/K/Q/J/T 大牌引牌不算）。

- 验证：`scripts/_finesse_commit_verify.py` 扩展为 8 组断言全过（含 ⑦同伙大牌引牌不触发、⑧♦K已出不再误判可飞）。

- **该修复尚未 git 提交。**

## 九、变更文件清单（均未提交）

- `config.py`：+8 行（三配置）

- `bridge/play_service.py`：+\~210 行（7方法 + DD/αμ 接入）

- `scripts/_finesse_structure_chklist.py`（新增）

- `scripts/_finesse_defer_verify.py`（新增）

- `scripts/_finesse_akj_verify.py`（新增）

- `scripts/_finesse_commit_verify.py`（新增）

- `scripts/_success_rate_compare.py`（新增，成功率口径实验）

- `scripts/_finesse_detect_test.py`（新增，两方法对比）

- 本文档（docs/，记录中）

## 十、BUG 修复（2026-08-31）：强制接应越权导致"AI出牌失败（已暂停）"

### 现象

实战牌局（南♠A7♥J83♦Q43♣QJ543 / 北♠QJ2♥QT754♦AJ♣A96，东♠T 领出）轮到南（庄家）出牌时前端报 **"AI出牌失败（已暂停，点击'继续'重试）"**，重试无效。

### 根因（两处叠加）

1. **敌人出牌被误判为"同伙启动飞牌"**：`_finesse_commit_check` 判定仅看"当前墩中存在飞牌花色牌"（`any(c.suit in fs_suits ...)`），不看是谁出的。东♠T（敌人）让 ♠（缺K，已识别为可飞花色）被当作"同伙已启动飞牌"。
2. **强制出的飞张不合法**：接应逻辑直接改选"全手牌中最大飞张"（♦Q），完全无视跟牌规则——当前墩领出♠，南**必须跟♠**（只能出♠A/♠7）。`play_card` 校验失败 → `_execute_ai_play` 返回 `success=False, error=None`（L2343 分支未带 error）→ 前端无详情文案。

### 修复（`_finesse_commit_check`）

- 新增**同伙身份判定**：仅当前墩中**同伙（PARTNERS\[turn]）出过飞牌花色牌**才算"启动飞牌"，敌人出的同花色牌不算。

- 新增**合法性过滤**：飞张只从 `state.get_playable_cards(turn)`（当前合法可出牌，已含跟牌规则）中选取；可出牌中无飞张 → 不强制。

- 修复后该局面正确返回 None（不干预），DD 正常出跟牌 ♠A/♠7。

### 验证

`scripts/_finesse_commit_verify.py` 扩展为 6 组断言：

- ①-③ 原 4♥ 接应场景仍强制 ♥J（未破坏）

- ④ 防守方角色不触发

- ⑤ **BUG 回归**：东♠T 领出、南须跟♠ → 强制接应不越权（返回 None）

- ⑥ `_dd_play` 集成（真实引擎 50 样本）：返回 ♠A（合法跟牌）、无飞牌接应标记

全部通过 ✓。后端已重启加载修复。

## 十一、启动判定放宽与接应选牌修正（2026-09-02）

### 用户指正的核心规则

用户通过多轮讨论澄清了飞牌接应的完整规则：

1. **启动判定**：同伙出**任何 < 对象**的牌都算启动飞牌（包括 Q 找 K、J 找 Q、T 找 J 等间张引牌），不再限于 ≤9 小牌。
2. **接应选牌**：

   - 敌方已出对象大牌（≥对象）→ 用**最小的高于对象的顶张**盖掉

   - 敌方未出对象：

     - 同伙领出 ≥10（Q/J/T 间张引牌）→ 出**最小**的低于对象的牌（不浪费飞张，如 AJx 中出 3 不出 J）

     - 同伙领出 <10（小牌找飞）→ 出**最大**的低于对象的牌（贴住对象的飞张，如 AJx 中必须出 J）

### 代码修改（`_finesse_commit_check`）

- **启动判定**：`rv < finesse_struct[suit]["对象"]`（替代原 `rv <= 9`）

- **接应选牌**：新增敌方出牌检测（`enemy_top`），按"对象是否已现 + 同伙领出大小"三分支：

  1. `enemy_top >= obj` → 出 `min(covers)`（最小顶张盖）
  2. `partner_rv >= 10` → 出 `min(below)`（最小飞张，保 J）
  3. `partner_rv < 10` → 出 `max(below)`（最大飞张，贴对象）

### 识别层跨花色串味 bug 修复（同日）

`_detect_finesse_struct` 原实现把**所有花色**已打出的 A/K/Q/J rank 混进同一个 set（`played_ranks`），再并入每个花色的 present——导致 **♠Q 打出会抹掉 ♥ 的"缺 Q"识别**（跨花色串味，漏报飞牌结构）。

修复：改为 `played_by_suit: {花色: set(rank)}` 按花色分别记录，present 只并入**同花色**的已出大牌。

验证（`scripts/_finesse_commit_verify.py` 新增 ⑫）：

- ♠ 与 ♥ 各自缺 Q → 均识别

- ♠Q 打出后 → ♠ 消除（Q 已现），♥ 仍识别（不受影响）✅

- 23/23 结构用例回归通过

### 验证

`scripts/_finesse_commit_verify.py` 扩展为 **11 组断言**：

- ①-⑧ 原有场景（4♥ 接应、防守方不触发、敌人领出不越权、DD 集成、顶张不算启动、已出大牌排除）

- ⑨ 同伙领 Q、敌方未出 K → 出 **♥3**（最小飞张）

- ⑩ 同伙领 Q、敌方出 K → 出 **♥A**（盖掉）

- ⑪ 同伙领小、敌方未出 K → 出 **♥J**（最大飞张贴对象）

全部通过 ✓。

## 十二、将牌不拖延 + 8飞9砸通用原则（2026-09-02）

### 需求（用户澄清）

- **将牌花色不拖延**（将牌需积极处理，清将/飞将优先）

- **8飞9砸是通用原则，不限于将牌和有将定约**：飞牌花色"被迫引发"（榜首为该花色牌）时，
  联手张数 ≤8 → 出飞张（间张）；≥9 → 出顶张（A/K 砸）

- 拖延链：非将牌拖延 → 将牌不拖延 → 被迫引发时 8飞9砸

### 代码修改

1. `config.py`：新增

   ```
   FINESSE_EIGHT_NINE_ENABLE = True   # 8飞9砸总开关
   FINESSE_EIGHT_NINE_RATIO = 0.90    # 改选牌相对成功率 ≥ 榜首 90% 才改选
   ```
2. `_apply_finesse_defer`（L2650）：入口剔除将牌花色（将牌不拖延）
3. 新增 `_apply_eight_nine_rule`（L2691，通用原则，含将牌）：

   - 榜首是该飞牌花色的牌时介入

   - 联手张数 ≤8 → 若榜首是"砸张"（>对象）且候选有"飞张"（10≤rv<对象）达标 → 改选飞张（8飞）

   - 联手张数 ≥9 → 若榜首是"飞张"且候选有"顶张"（>对象）达标 → 改选顶张（9砸）

   - 比值阈值保护（FINESSE\_EIGHT\_NINE\_RATIO）：差距大不干预

   - 写入 `full_output["八九原则"]`
4. 接入两条引擎路径：`_dd_play` / `_alpha_mu_play` 在接应→拖延后调用

### 验证

`scripts/_finesse_defer_verify.py` 扩展为 **11 组断言**（原7组 + 新增⑧-⑪）：

- ⑧ 联手8张缺Q 榜首♥A(砸) → 改选 ♥J（8飞）✅

- ⑨ 联手9张缺Q 榜首♥J(飞) → 改选 ♥A（9砸）✅

- ⑩ 差距大（比值0.808<0.90）→ 不干预，保留榜首 ✅

- ⑪ 将牌花色同样适用8飞9砸（通用原则，不受"不拖延"影响）✅

全部通过 ✓。结论：边花先拖延（中断即停），无法拖延被迫引发时按张数飞/砸；将牌不拖延但 8飞9砸 依然生效。

### 8飞9砸 补充：AK 判据（同日追加）

用户指出关键点：**9 砸（拔 A/K 赌 Q 跌落）要求 A、K 两张顶张都在我方庄家/明手**。

- 若 AK 缺一（只有 A 无 K / 只有 K 无 A）→ **砸不动 Q，即使 9 张也应飞**

- 若 8 张 → 本就走飞（无 AK 要求）

- 检验口径：`own_ranks` 含 14(A) 且 13(K) → `ak_ok=True` 才允许 9砸；否则按 8飞

`_apply_eight_nine_rule` 修改：`should_garrison = trump_cnt >= 9 and ak_ok`；
提示与 `full_output["八九原则"]` 新增 `AK齐全` 字段。

验证脚本扩展为 **13 组断言**，新增：

- ⑫ 联手9张缺Q 但只有K无A（AK不齐）→ 仍改选 ♥J（8飞），`AK齐全=False` ✅

- ⑬ AK 齐全时 9 张仍 9砸（回归）✅

## 十三、9张缺K持AQ：先砸后飞（2026-09-03）

### 需求（用户澄清）

8飞9砸 需建立 **9 张持 AQ、缺 K** 的场景，打法为**先砸后飞**：

- 联手 9 张、缺 K（对象=K=13），我方持 A + Q → **先拔 A 砸**，K 未跌落再飞 Q

- 与既有"缺Q需 AK 齐全才砸"不同：缺 K 场景不需要 K，只要有 Q 可接力即可先砸

### 规则判定（\_apply\_eight\_nine\_rule 判据拆分）

| 场景           | 对象    | 顶张判据      | 动作                 |
| ------------ | ----- | --------- | ------------------ |
| 缺Q/J         | 12/11 | A+K 齐全    | 9砸（拔 A/K）          |
| 缺K 持 A+Q     | 13    | 有 A 且 有 Q | 9砸先飞（拔 A，K 未落再飞 Q） |
| 缺K 无 Q（如 AJ） | 13    | 无 Q 接力    | 砸后无法接力 → 仍 8飞      |
| 联手 ≤8        | 任意    | —         | 8飞                 |

`should_garrison` 按对象区分：`obj==13 → 张数≥9 且 has_a 且 has_q`；
否则保持 `张数≥9 且 ak_ok`。

- 新标注 `原则="9砸先飞"`（AK齐全=False），提示 `（缺K持AQ：先砸A再飞Q）`

- 缺Q/J 场景不受影响：AK 缺一仍按 8飞（示例：9张只有K无A、缺Q → 飞）

### 验证

`scripts/_finesse_commit_verify.py` 新增 2 组断言（脚本用例13/14），全部通过 ✓：

- 用例13：9张缺K持AQ 榜首♥Q（飞K）→ 改拔 ♥A（原则=9砸先飞）
- 用例14：9张缺K无Q（AJ）榜首♥A → 改飞 ♥J（原则=8飞，AK不齐只能飞）

### 9砸后续：A已砸、K未现 → 保护间张（6NT例牌实测后追加）

用南♠AK32♥K32♦432♣KQJ / 北♠54♥AQJ♦AQT987♣A2（西首攻♠Q，6NT）实测：

- 墩7 北♦A（9砸先飞 ✓）
- **墩8 北出 ♦Q 被东♦K 吃** → 东 KJ 藏 K，Q 主动送死（丢方块一个赢墩）

修复（两个点）：

1. `_detect_finesse_struct`：控制张/飞张改按 **present（手牌 ∪ 已出）** 计算，不再只算手牌。A 已兑现后"缺K先砸后飞"结构仍成立，供后续规则识别
2. 新增 `_apply_nine_cash_followup`（接入 _dd_play / _alpha_mu_play 飞牌链尾部）：A 已砸、K 未现、榜首又是间张（10≤rv<对象）时，从候选改选**该花色最小牌喂 K**，保留下方间张（Q 等）继续作飞张；比值 ≥0.95 才改选；K 已现（跌落）不干预

验证：`_finesse_commit_verify.py` 新增用例15（A已砸K未现→改出♦7保留Q）、用例16（K已现→不干预），16 组断言全部通过。实测对局墩9 由旧"出♦Q被K吃"变为"出♦7喂K"。

### 9张判据按"墩"结算（web 实测后修正，同日）

web 实测同一例牌发现：南♦4 领出后强制接应锁北出♦Q，"9砸先飞"未覆盖。根因：`_apply_eight_nine_rule` 联手张数按"在手牌"算只剩 8 张（南已出1张）→ 误判 8飞。

修正（`_apply_eight_nine_rule`，用户语义）：

- **首墩**（该花色此前 tricks 中未出过，`prev_suit_played==0`）→ 联手数 = 在手 + 本墩我方已出（初始 9 张），8飞9砸只在此刻生效——拔 A 的时机只有一次
- **非首墩**（该花色此前已出过）→ 联手数 = 剩余在手张数，9砸不再介入（避免"A 已砸仍按9张处理"的矛盾），间张保护交给 `_apply_nine_cash_followup`

验证：用例⑰（首墩同伙已领1张→仍判9砸先飞改♦A）、用例⑱（A已砸非首墩→八九原则不介入），18 组断言全部通过。

## 十四、飞牌启动重构（2026-09-07 ~ 2026-09-08，v1.70）

> 本轮把飞牌干预从"识别→拖延→接应→续飞"改为"**探针检测 + 显式验证 + 窗口期主动启动**"三层，并废弃整个拖延策略。核心问题：单步贪心看不到跨墩飞牌收益；方向性（谁先出/谁过手）与值问题（出哪张）是两个维度。

### 14.1 拖延策略废弃（用户决策）

- **理由**：坐庄方明手牌可见、防守方为采样平均，引擎单步贪心不会因拖延换花色"获得信息后改判"——拖延只有代价无收益。
- 删除：`_defer_candidates`、`_is_danger_suit`（护张保护随拖延废弃一并删除）、`_is_finesse_struct_card`、入口领出拖延分支；配置 `FINESSE_RATIO_RISK`/`FINESSE_LOSE_TRIGGER`/`FINESSE_DEFER_WIN_MIN`。
- **纠错**：9/7 只删了配置，方法与领出分支当时仍残留（`_defer_candidates`/`_is_danger_suit`/`_is_finesse_struct_card`），9/8 才全部清干净。教训：删功能要一次删完（调用点+方法+配置），不要分批。

### 14.2 窗口期主动启动飞牌（`_probe_lead_finesse_prefer`）

- 探针识别出结构（Δ≥0.4）即"窗口期"：此刻飞最有价值。领出方为庄/明手、引擎榜首是其他花色时，主动改出该花色路线启动飞牌，避免"顶张先打完、窗口关闭后飞牌无收益"的死锁。
- **门槛关键决策：不走 0.95 比值**——0.95 评估"这张牌 vs 榜首"的单步值，单步贪心眼里顶张恒高于飞张小牌，会系统性扼杀所有主动启动；探针 Δ（分桶均值差）本身是跨墩度量，可直接作启动门槛。
- 分流：联手≥9 → 按砸/飞分流（应砸出顶张；应飞顶张方回手、非顶张方直飞）；<9 → 只飞不砸，顶张方无稳赢回手牌则不强制（尊重引擎）。
- 输出 `full_output["窗口期启动"]`。

### 14.3 顶张侧过手启动（`_apply_lead_transfer`）

- 方向性问题：顶张方（持 A/Q）自己领出该花色方向错误——出顶张是砸、出间张烧结构，飞牌须由无大牌的队友侧首次引飞。
- 触发：当前侧探针未识别结构时，"假设从队友侧出一次牌再探测"——真的以队友为领出方跑一次 DD 评估（`dd_search.search(state, perspective=partner, actual_turn=partner)`，新增覆盖参数），取队友侧 `finesse_probe`。
- **口径一致性：队友侧探测也是探针法 + 同一 Δ≥0.4 门槛，模板法只归 αμ 引擎**（用户明确要求，不许用模板放水）。
- 结构成立 + 当前方是该花色顶张方 + 队友有飞张小牌（<对象）+ `_cash_reentry`（原 `_nine_cash_reentry` 更名统一）有稳赢过手牌 → 改出无关花色小牌过手给队友 → 写 `finesse_flow` 标记 → 此后由流程延续正常接管。
- 输出 `full_output["领出过手"]` / `领出飞牌.引发=True 说明=顶张侧过手启动`。

### 14.4 流程标记对象化（finesse_flow 存对象）

- **根因**：finesse_flow 原存"进行中"字符串，`_apply_flow_continuation` 清理逻辑用"当墩 finesse_struct 缺失"→ 判空 → 直接清除流程。顶张侧回手后探针一时 Δ<0.4，流程被误清 → 飞牌中断（6♠ 例回手后北被放行乱出）。
- 修复：写入时存对象 int（`state.finesse_flow[s] = obj`）；清理只按"对象现身 / 花色已死"判定，与当墩探针无关。
- 配套：`_apply_lead_finesse_check` 探针空时若 flow 有活动仍先走流程延续，再试过手，最后才"无飞牌结构"。

### 14.5 探针法：分桶敏感性 + Δ 阈值 0.4

- DD 引擎飞牌识别**只用探针法**：probe 键存在（无论是否为空）只用探针结果，判空即无飞牌结构，不回退模板法（模板法会误判"缺K即可飞"，如南直接领出 Q 的假飞场景）。
- **阈值 0.5→0.4**：基于真实触发时点（游戏进行中我方赢下上一墩后领出）实测调整——8张样本平均 Δ=0.76、9张平均 Δ=0.86，原 0.5 使 8/9 张识别近乎全线失能。
- **教训（测试时点）**：在"整副初始+强改 current_player"下测试探针会系统性压低 Δ 值；正确测试应在真实触发时点进行。

### 14.6 可飞性校验（`_probe_struct_playable`）：探针检测 + 显式验证两层

- **问题**：探针 Δ 高只说明"位置敏感"，不必然构成可飞结构。6NT 例（南♠AK32♥K32♦432♣KQJ / 北♠54♥AQJ♦AQT987♣A2）探针报 ♠T Δ=0.4 压线，被误当飞牌结构强改出 ♠2——但 ♠ 联手只有 AK+小牌，没有任何 <T 的间张，无张可飞，只能是砸兑现。
- 校验：我方现手（庄+明）该花色须有**可飞间张（<对象 且 ≥10）** 与**上方控制（>对象）**，否则跳过该花色。
- **主路径与顶张侧过手路径同口径共用**（过手路径此前从队友侧探针构建结构时漏了该校验，同类假结构 ♥K32+AQJ 缺 T 也会触发过手，seed42 实测抓到）。

### 14.7 探针 3 张滑动窗口（修正固定 AKQJT 五张集）

- **用户设计原意**：默认检测对象是 AKQ；出掉一个、下移补一个（出掉的就不检测），一直保持检测三张牌。例：飞 K 且 K 已出 → 窗口变 AQJ；A 也出 → 变 QJT。这样残局阶段小牌（J/T）也能被飞，自然扩展飞牌范围。
- **实现偏差教训**：上轮把"动态下移"扁平化实现为固定 5 张集 `{A,K,Q,J,T}`，每个花色一次性监控全部缺失大牌——违背了"3 张滑动窗口"语义，且对所有花色一律下移，导致 ♠AK+xx 缺 T 类"J/T 位置敏感"假阳性（♠T Δ=0.4 压线误报）。
- 最终实现（`dd_search._honor_missing_of_state`）：
  - 非飞牌花色：窗口固定 `[A,K,Q]`（打出即排除、不补位）
  - 飞牌花色（`state.finesse_flow` 中活动流程的花色）：窗口取 AKQJT 中前 3 张未打出者，出掉一张、下移补一张
  - **下移只针对飞牌花色，不作用于所有花色**
- 配套：删除失效 `_R2V_PROBE`；`play_types.finesse_flow` 类型标注更正为 `Dict[str, int]`。

### 14.8 验证

- 滑动窗口单测（`tests/_tmp_probe_window.py`）：无流程时 ♠{Q}、♦{K}；♦ 流程中 K 未现仍 {K}；K 出后窗口滑到 {J}；非飞牌花色 ♠Q 出掉不补位 → PASS。
- `tests/_tmp_fly_check.py`：♠(T) 无间张被排除、♦(J) 有 T 间张保留 → PASS。
- seed42 6NT 回归：probe 只报真实 ♦(K)，8飞9砸先砸A再飞Q，12 墩做成；无假 ♥T 过手、无 ♠T 启动。

