# αμ+LLM 引擎开发记录

> **开发日期**: 2026-07-12
> **核心文件**: `bridge/play_service.py`
> **测试文件**: `tests/test_vector_grouping.py`, `tests/test_grouping_examples.py`
> **状态**: 阶段A（分组重构）+ 阶段B（plan结构化）已完成；后续阶段 待实施

---

## 一、引擎设计初衷

### 1.1 问题背景

αμ搜索是桥牌做庄的强大引擎，通过多世界采样 + 双明手（DDS）评估，能解决PIMC的strategy fusion和non-locality问题。但αμ有两个固有局限：

1. **概率驱动**：αμ按成功率选牌，但实际打牌有时必须找**唯一成局路线**，不论概率
2. **缺乏战术语义**：αμ只知道"出这张牌60%成局"，不知道"这是飞牌/清将/建立长套"

### 1.2 设计理念

**αμ+LLM** = αμ搜索（提供精确的DDS等价分组和成功率） + LLM审查（提供战术语义和输墩分析）

分工：
- **αμ负责"哪些牌等价"**：通过best_vector分组，vector相同=DDS等价
- **LLM负责"选哪组+为什么"**：基于输墩分析、战术评估、将吃风险，选择最优组

核心原则：**充分利用αμ搜索结果**，LLM只在αμ无法明确判断时介入。

---

## 二、开发过程与关键讨论

### 2.1 阶段一：总体review引擎缺陷

用户要求总体review αμ+LLM引擎的缺陷。我提出8条优化建议，其中：

**建议5的错误**：我说"组内出牌规则在飞牌场景下是错的"。
- **用户纠正**："如果是连接张，每张都是等价的，不会出现你说的在许多场景下都是错的"
- **教训**：连续张DDS等价，出哪张都一样，组内出牌规则没问题

### 2.2 阶段二：分组逻辑重构（核心，多轮迭代）

#### 2.2.1 第一次尝试：按战术意图分组 → 被否

我提出按战术意图（飞牌、清将、长套建立等）分组。
- **用户质疑**："为什么只有连续组才有战术？单张♠Q完全可以是飞牌"
- **结论**：战术意图依赖局面，程序无法准确判断，分类逻辑根本性错误

#### 2.2.2 关键突破：用户提出用best_vector分组

> **用户原话**："连续组肯定要保留的，而且我认为它们的vector肯定是一样的，没有必要校验。然后，不连续牌之间也是可以分组的，就是利用vector，如果vector完全一样，就可以化为一组。比如2，3，5，很大概率出哪个都一样。大牌可能不一样，但是如果vector一样，起码说明在采样空间里是相同的，可以化为一组。**既然选择了αμ，我们还是要充分利用搜索的结果。**"

**best_vector = αμ搜索的产物，vector相同 = DDS在采样空间等价 = 一组**

#### 2.2.3 三层分组规则的演化

**第一版**：vector相同 + 将牌/非将牌分开 + 按rank区间拆分。

**用户反馈**："S小牌和SQ为什么不分两组" → rank区间拆分粒度不够。

**第二版**：同花色内按[2-7]/[8-10]/[J-A]拆分，跨区间连续(7-8, T-J)不拆。

**用户反馈**："干脆把不同花色都分开分组好了" → 最终第二层改为按花色拆分（不再按将牌/非将牌）。

#### 2.2.4 最终分组规则（`_group_candidates_by_vector` + `_split_by_rank_tier`）

| 层次 | 规则 | 例 |
|------|------|-----|
| 第0层 | 筛选 success_rate > 0 | |
| 第1层 | best_vector 相同 → 同一批（DDS等价） | ♠2♠5♠Q 与 ♣3 不同批 |
| 第2层 | 同一批内按**花色**分开 | ♠2♠Q 与 ♣3 不同组 |
| 第3层 | 同花色内按 **rank区间** [2-7]/[8-10]/[J-A]拆分 | ♠2♠5 一组，♠Q 另一组 |
| 连续豁免 | 跨区间但连续 → 不拆 | ♠7♠8、♠T♠J 不拆 |
| 无截断 | 所有 >0% 都保留给LLM审查 | |

### 2.3 阶段三：触发条件调优（50% → 30% → >0% + 15%截断 → 全保留）

**50%阈值**（初始）：太高，48%的战术分叉点被跳过。

**30%阈值**（用户提出）：
> "我甚至觉得降到30%都可以。30%说明有相当数量的情况会成功。而且里面可能包含当前牌局下唯一的成局线路。αμ靠的是概率，但是实际的打牌，有时候不得不去寻找唯一的成局路线，而不论概率。"

**>0% + 15%截断**（用户提出）：
> "LLM评估出牌的阈值不应该固定为30%，而是针对任何存在的机会，也就是大于0%的出牌都应该加入分组评估。对于所有的组，仍然保留15%的间距，多个分组按成功率排序，如果某两个组之间存在15%的差距，可以把较低成功率及以下的分组排除。"

**全保留**（用户最终决定）：
> "把15%阶段的分组要求去掉，所有有机会成局的情况都应该考虑。"

**最终**（`_should_trigger_llm`）：组数 ≥ 2 即触发，不设成功率阈值。

**复调优**（用户提出，DD中局与αμ残局差异化）：
> "dd中局阶段保留15%截断，αμ取消，保留全部。"

实现：`_should_trigger_llm` 新增 `gap_threshold` 参数。
- DD中局（`_dd_llm_play`）：传默认 `gap_threshold=0.15`，保留顶差15%截断。
- αμ残局（`_alphamu_llm_play`）：传 `gap_threshold=None`，仅看组数（≥2即触发），保留全部候选。

### 2.4 阶段四：绝望模式

**需求**（用户提出）：
> "如果vector全为0，也就是αμ认为没有打成定约的机会，这时也要启动LLM审查，目的是尽可能多拿墩少宕。也就是说这一墩要尽量赢。"

**实现**：
- 检测所有 candidates 的 success_rate == 0 → `desperation_mode = True`
- 新增 `_build_desperation_groups`：按花色+rank区间分组，按 avg_tricks 排序
- 新增 `_build_strategy_text`：绝望模式专用prompt（3维评估，目标从"成约"切换为"多拿墩少宕"）
- 系统提示"αμ搜索已判断无成约机会，目标是尽可能多拿墩、少宕"

### 2.5 阶段五：去除战术预设

**问题根因**：分组按rank区间拆分，隐式暗示"小牌组=清将、大牌组=飞牌"。但实际：
- 小牌也可以飞牌（从明手出♠2飞对方的♠K）
- 大牌也可以清将（出♠A清将）
- 边花也有飞牌需要（飞边花K/Q/J）

**讨论过程**：
- 用户指出："出小牌也可以开始飞牌，而小牌只赋予了清将组。另外，边花也有飞牌的需要。"
- 最初考虑改变分组方案（去掉rank区间拆分），但用户决定："不要改变分组方案，去掉给每个组赋予技术功能，由LLM去决定是清将还是飞牌。"
- 最终在prompt中强调"分组不预设战术——同一组牌既可能用于飞牌，也可能用于清将"。

**同时删除死代码**：
- `_classify_tactic`（87行，为每组标注战术标签，prompt已不用）
- `_split_by_tier`（48行，旧版分组逻辑，被`_split_by_rank_tier`替代）

### 2.6 阶段六：LLM事实性错误修复

#### 2.6.1 错误1：混淆庄家明手牌

LLM说"原计划第一步出♦2"，但♦2在庄家手里，当前轮到明手出牌。
**修复**：prompt强调"当前出牌方"和"必须从该位置手牌出牌"。

#### 2.6.2 错误2：数输墩方法错误

LLM庄家明手分开数，把♦A当输张，遗漏将牌输墩♠K♠T。
**修复**：prompt明确"从庄家方整体数（庄家+明手一体）"，"A是赢张不是输张"。

#### 2.6.3 错误3：将牌计数错误

LLM说"庄家方将牌合计13张"（不可能）。此前只给计算公式，LLM直接断言而非计算。
**关键决策**：程序直接计算将牌统计，注入prompt，LLM只需读取。新增 `_format_trump_analysis`。

#### 2.6.4 错误4：战术标签误导LLM

标签"飞将牌"导致LLM说出♠A后丢了飞牌机会（实际出♠A是拔大牌不是飞牌）。
**用户建议**："去掉分组分类名称，避免误导，交给LLM去分析"。

#### 2.6.5 错误5：红桃输墩未数出

LLM说"边花无输墩"但红桃没有A。
**修复**：prompt增加"如果庄家方某花色没有A，则对方有A，该花色至少有1个输墩"。

### 2.7 阶段七：Prompt系统重构（对标LLM引擎 + 合并清理）

#### 2.7.1 对比LLM引擎，借鉴改进

LLM引擎（`PLAY_DECLARER_PROMPT`）有αμ+LLM缺失的多个要素：
- 叫牌过程（推断大牌位置和飞牌方向）
- 将牌清完状态
- 无将定约的赢墩分析（五问）
- 候选取对比（5维评估结构）
- 预测回牌
- 进张管理
- 一致性约束

#### 2.7.2 合并死代码 `_llm_strategic_review`

该函数从未被调用（只有定义，无调用点），是早期两阶段LLM审查设计的残留。将其有用片段合并到 `_llm_group_review` 后删除。

#### 2.7.3 最终Prompt结构

**System Prompt**：
- 正常模式："桥牌做庄专家，先数输墩/赢墩再分析战术选组"
- 绝望模式："αμ判定无成约机会，目标是多拿墩少宕"
- 关键战术列表：飞牌/将吃/进张管理/长套建立/终局打法

**User Prompt 各段**：

| 段 | 内容 |
|---|---|
| 局面信息 | 定约、庄家、明手、出牌位置(第X家)、墩数进度、将牌清完状态 |
| 叫牌过程 | bidding_sequence（推断对方大牌位置，辅助飞牌方向） |
| 基本规则提醒 | A>K>Q>J>10...，跟牌，将牌 |
| 手牌 | 庄家和明手的当前手牌 |
| 将牌统计 | 程序计算（庄X张、明Y张、对方Z张、对方大牌明细） |
| 对方关键大牌 | 庄家方手里没有的 A/K/Q/J/T |
| 已完成墩 + 已出牌 | |
| 可选出牌组 | DDS等价分组，不预设战术 |
| Plan段 | 现有plan校验（4个动态失效触发条件）/ 新plan制定 |
| 战略分析 | 见下方分支 |
| 一致性约束 | 所选组必须与战略分析结论一致 |
| JSON输出 | {group, plan, steps, plan_valid, reason} |

#### 2.7.4 战略分析分支（`_build_strategy_text`）

| 正常模式（有将） | 正常模式（无将NT） | 绝望模式 |
|---|---|---|
| 1. 数输墩 | 1. 数赢墩（五问） | 1. 数赢墩（多拿墩少宕） |
| 2. 识别飞牌机会（缺K/Q/J→飞牌） | 2. 识别飞牌机会 | 2. 识别赢墩机会 |
| 3. 5维评估 | 3. 5维评估 | 3. 3维评估 |
| (1)战术意图 (2)消除能力 | (1)战术意图 (2)消除能力 | (1)战术意图 (2)赢墩数 |
| (3)预测回牌 (4)失败代价 (5)将吃风险 | (3)预测回牌 (4)失败代价 (5)将吃风险 | |
| 4. 进张管理 | 4. 进张管理 | — |
| 5. 已选牌与战术关系 | 5. 已选牌与战术关系 | — |
| 6. 选最优组（不只看成功率） | 6. 选最优组 | 3. 选赢墩最多组 |
| 一致性约束 | 一致性约束 | — |

"有将定约还包括清将"仅在有将且非绝望模式下显示。

#### 2.7.5 飞牌机会识别（优先级高）

prompt中新增的飞牌优先提示：
- 对照"对方关键大牌"列表，检查每个候选组所在花色是否缺K/Q/J
- 如果某组所在花色对方有K/Q/J → 优先考虑飞牌或其他战术，不要简单选择αμ成功率最高的组
- 特别提示"飞牌失败通常只丢1墩，但错过飞牌机会可能导致定约必宕"

#### 2.7.6 一致性约束

```
你选择的组必须与"战略分析"中认定的最优战术一致。
若分析认定需要飞牌，所选组必须能启动飞牌；
若认定需要树立长套，所选组必须能启动长套建立。
若所选组与战略分析结论矛盾，视为严重推理失败，必须重新选择。
```

### 2.8 阶段八：Plan生命周期管理

#### 2.8.1 Plan步骤完成跟踪（新增 `_mark_step_completed`）

- 出牌后自动匹配plan中第一个未完成步骤的action字段
- 匹配则标记 `completed = True`
- 打印日志 `[αμ+LLM] plan步骤X完成`

#### 2.8.2 Plan失效检测（`_check_plan_invalidation`）

四个条件，满足任一即失效：
0. **所有步骤都已完成** → 计划执行完毕
1. **plan中提过的大牌已出** → ♠K已在完成墩或当前墩出现
2. **plan创建超过4墩未更新** → 时间过久
3. **未完成步骤的precondition中关键大牌已出** → 前提不满足

### 2.9 阶段九：use_reasoning透传修复 + 徽章修复

**Bug 1 - use_reasoning未透传**：
- 前端发送 `use_reasoning=True`，但 `_alphamu_llm_play` 收到 `False`
- 根因：L277旧代码 `self._alphamu_llm_play, state)` 缺少第3个参数
- 修复：`self._alphamu_llm_play, state, use_reasoning)`

**Bug 2 - 徽章显示"快答"而非"思考"**：
- `actual_model = pm_raw`（无`::reasoning`后缀），前端无法判断思考模式
- 修复：`actual_model = f"{pm_raw}::reasoning" if use_reasoning else pm_raw`
- 前端徽章显示形如 `αμ+V4-Pro·思考` / `αμ+V4-Pro·快答`

**UI改动**：
- SettingsPanel：αμ+LLM引擎允许选择LLM型号和快答/思考切换（之前disabled）
- PlayDetailPanel：输出界面顶部徽章显示具体LLM型号和模式

---

## 三、当前完整框架

### 3.1 主流程：`_alphamu_llm_play`

```
每墩调用一次
  │
  ├─ Step 1: αμ搜索 → _alphamu_full_play() → candidates + best_vector
  │
  ├─ Step 2: 分组（两路）
  │     ├─ 正常路径：_group_candidates_by_vector()
  │     │     → 筛选 success_rate>0
  │     │     → vector → 花色 → rank区间
  │     │     → 按 success_rate 降序排列编号（无截断）
  │     │
  │     ├─ 触发条件：_should_trigger_llm() → 组数≥2
  │     │
  │     └─ 绝望路径：all candidates success_rate==0
  │           → _build_desperation_groups()
  │           → 花色+rank区间分组，按 avg_tricks 降序
  │           → desperation_mode = True
  │
  ├─ 角色限制：仅庄家方（declarer/dummy）
  │
  ├─ Step 3: Plan失效检测 → _check_plan_invalidation()
  │     四个失效条件（全完成/大牌已出/超4墩/前提不满足）
  │
  ├─ Step 4: LLM审查 → _llm_group_review()
  │     → 构建完整prompt（局面/叫牌/手牌/将牌统计/对方大牌/分组/plan）
  │     → _build_strategy_text() 分支（正常/绝望）
  │     → DeepSeek chat_json(temperature=0.3, thinking=use_reasoning)
  │     → 返回 {group, plan, steps, plan_valid, reason}
  │
  ├─ Step 5: 保存plan → _build_plan_from_review()
  │
  ├─ Step 6: 出牌 → 组内选牌（庄家选最大rank / 明手选最小rank）
  │     → _mark_step_completed() → 标记匹配的plan步骤完成
  │
  └─ 回退：LLM选组失败 → 保留αμ纯结果
```

### 3.2 关键组件清单

| 组件 | 行号(约) | 职责 |
|------|----------|------|
| `_alphamu_llm_play` | L1236 | 主入口 |
| `_group_candidates_by_vector` | L1469 | 三层分组 |
| `_split_by_rank_tier` | L1567 | 花色+rank区间拆分 |
| `_build_desperation_groups` | L1519 | 绝望模式分组（按avg_tricks） |
| `_should_trigger_llm` | L1450 | 触发条件（≥2组，DD带15%截断/αμ全保留） |
| `_check_plan_invalidation` | L1370 | plan失效检测（4条件） |
| `_mark_step_completed` | L1426 | 标记plan步骤完成 |
| `_llm_group_review` | L1837 | LLM审查（主prompt + 调用） |
| `_build_strategy_text` | L2009 | 正常/绝望strategy文本 |
| `_build_plan_from_review` | L1344 | 结构化plan存储 |
| `_format_trump_analysis` | L886 | 程序计算将牌统计 |
| `_format_missing_key_cards` | L867 | 对方关键大牌列表 |
| `_extract_rank` | L1462 | ♠2→2, ♥J→11, ♣A→14 |
| `_rank_tier` | 约L1540 | ≤7=low, 8-10=mid, ≥11=high |

### 3.3 已删除代码

| 函数 | 原因 |
|------|------|
| `_classify_tactic` (87行) | 战术标签不再使用，prompt不预设战术 |
| `_split_by_tier` (48行) | 旧版分组逻辑，被 `_split_by_rank_tier` 替代 |
| `_llm_strategic_review` (136行) | 从未被调用，有用片段已合并入 `_llm_group_review` |

---

## 四、后续待实施

| 阶段 | 内容 | 说明 |
|------|------|------|
| C | 事后校验 | LLM选牌后程序校验是否合理，不合理则回退αμ |
| C | 困难局面处理 | 将牌全清后、残局（≤4墩）的精确计算 |
| D | 防守方扩展 | 当前仅庄家方使用αμ+LLM，防守方残局（≤6张）可引入LLM审查，偏重花色全明计算+转攻方向判断 |
| E | 复盘日志 | 记录LLM决策全过程，便于事后分析 |
| F | 分组解释 | 从best_vector反推采样分布，给LLM解释"为什么等价" |
| G | 增量plan传递 | 残局阶段只传最近未完成的3步，减少prompt长度 |
| H | LLM选组失败诊断 | 回退分支中记录完整review dict到full_output |
| I | plan步骤解析健壮性 | 短句过滤（≤5字符跳过） |

---

## 五、关键决策与教训

### 5.1 关键决策

1. **用best_vector分组**（用户提出）：充分利用αμ搜索结果
2. **不设成功率阈值**（用户提出，αμ）：所有有机会的情况都让LLM审查；DD中局仍保留15%截断
3. **程序计算将牌统计**（我提出）：LLM数牌不可靠
4. **分组不预设战术**（用户提出）：同组大牌小牌都可能用于多种战术
5. **绝望模式**（用户提出）：αμ认为无机会时也要审查，目标切换为多拿墩少宕
6. **飞牌机会优先识别**（我提出）：对照对方大牌，不盲信αμ成功率
7. **将牌清完仅在<2时说明错了**（用户纠正→prompt改写）：无将定约不提清将

### 5.2 教训

1. **LLM数牌不可靠**：给公式也没用，关键事实必须程序计算后注入prompt
2. **充分利用搜索结果**：best_vector是αμ的核心产物
3. **战术预设是危险的**：rank区间不应暗示战术类别
4. **prompt的对标学习**：LLM引擎的优化经验可借鉴到αμ+LLM引擎
5. **死代码必须清理**：`_llm_strategic_review` 存在但从未被调用，容易误导理解

---

## 六、测试

### 6.1 `tests/test_vector_grouping.py`

13个测试用例，覆盖：
- 基本vector分组、非连续vector相同合并
- 连续张跨区间（T-J-Q）不拆分
- rank区间拆分（同花色大小牌分开）
- 不同花色不合并
- 7-8、T-J跨区间连续不拆分
- 5-T跨区间不连续拆分

### 6.2 `tests/test_grouping_examples.py`

10个典型分组实例验证脚本，覆盖飞牌vs清将、跨区间连续、不同花色分开、0%过滤、15%截断等场景。

---

**文档结束。**
